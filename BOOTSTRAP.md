# BOOTSTRAP：装载协议（Claude 读到本文件时按此执行）

你被用户显式要求为当前项目装载协作基线。**严格按以下五步执行，不跳步。**

## 第 0 步：护栏（先于一切）
- 本仓库任何内容**永不写入 `~/.claude/`**（全局目录）。
- 当前工作目录若是 `E:\A`（或用户声明的隔离项目）：**拒绝安装模式**；临时模式下**任何工作产物（工单/笔记/调研文件）也一律不得写入该目录**——先问用户★工作目录放哪里（建议 `E:\调研\<任务名>` 一类独立目录），产物全部落那里。隔离项目的盘面必须保持"仿佛本次装载从未发生"。

## 第 1 步：收集项目简介
用户的激活句通常自带项目简介。若缺失，问齐三件事（一次问完，**加粗带★**）：
1. ★项目类型与目标（做什么？给谁用？）
2. ★预期规模与周期（一次性小事 / 几天的小工具 / 周级以上正式项目？）
3. ★是否用 Codex 协作（**默认建议用**；用户未提时必须主动问这一句）

## 第 2 步：按判定表拟装载清单
| 判定 | 装载 |
|---|---|
| 一切情况 | `core/CLAUDE.md`（常驻件=宿主机制+合同）＋`core/原理与机制-Claude.md`（按需件=原理库+机制，上任通读一次；源在 `core/src/`，由 `render.py` 生成） |
| 中档及以上（多步/跨会话） | `modules/workflow` + `modules/evidence` |
| 简介提到游戏/玩法/原型/关卡/事件设计 | `modules/game-dev` |
| 简介以调研/查明/取证/排查为主要交付 | `modules/research` + `modules/evidence` |
| 用 Codex（默认是，除非用户明确说不用） | `modules/codex-collab` |
| 用户要把项目迁到另一模型家族/换总窗（可在项目中途单独装载） | `modules/migration` |
| 重档（正式项目） | `templates/` 四模板拷入项目 `docs/` |

**向用户复述拟装载清单与判定理由，等确认。**自动判断，不自动生效。

## 第 3 步：问装载模式（二选一）
- **临时**：仅本会话遵守（把选中文件读入本会话），不落任何盘；
- **安装**：写入本项目——`core/CLAUDE.md` → 项目根 `./CLAUDE.md`；`core/原理与机制-Claude.md` → `./docs/基线-原理与机制.md`（已存在则征得同意后追加为独立段落）；选中模块 → `./.claude/skills/<name>/SKILL.md`；重档模板 → `./docs/`。此后该项目的新窗口自动生效（作用域仅限本项目目录）。

## 第 4 步：执行并自述
执行后向用户复述：已装载哪些文件、当前任务档位判定、**当前协作档位(一/二)**、哪些条款现在生效。若为安装模式，提示"以后本项目更新基线=重说激活句选更新（会显示 diff）"。

## 第 5 步：更新流（当用户要求"沉淀/更新基线"时）
- 把项目里长出的通用经验改写为**去项目化**的条款（剥离专有名词），提交回本仓库（commit 信息说明来源与理由）；
- 修改既有条款须保住原条款的效力等级，冲突处向用户报告而非静默折中。
