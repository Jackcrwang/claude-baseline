# BOOTSTRAP：Codex 装载协议

用户已显式要求为当前项目装载协作基线。按以下五步执行；先展示拟议，再生效。

## 第 0 步：护栏

- 不写 `~/.codex/`，不改变全局配置，不安装依赖。
- 若当前目录是用户声明的隔离项目（当前为 `E:\A`），拒绝安装模式，只能临时装载；临时模式下任何产物（工单/笔记/调研文件）也不得写入该目录——先问用户 **★工作目录放哪里**（建议 `E:\调研\<任务名>` 一类独立目录）。隔离项目的盘面必须保持“仿佛本次装载从未发生”。
- 先读取当前目录及上层生效的 `AGENTS.md` / `AGENTS.override.md`，识别冲突、现有局部规则和可写边界。现有文件不覆盖：安装前展示 diff 与合并位置。

## 第 1 步：收集项目简介

激活句已有的信息不重问。若缺失，一次问齐会改变装载结果的最少信息：

1. **★项目类型、目标与主要用户是什么？**
2. **★规模与周期属于一次性小事、数日多步任务，还是周级正式项目？**
3. **★是否启用 Claude 跨家族审查？** 小项目默认关闭；质量攸关且独立第二视角有价值时建议开启。Codex 自带子代理无需另装。

## 第 2 步：拟装载清单

| 判定 | 拟装载 |
|---|---|
| 一切项目 | `core/AGENTS.md` → 项目根 `AGENTS.md` |
| 中档及以上 | `modules/workflow/SKILL.md` |
| 游戏/玩法/原型/关卡/事件设计 | `modules/game-dev/SKILL.md` |
| 调研/查明/取证/排查是主要交付 | `modules/research/SKILL.md` |
| 用户确认启用跨家族审查 | `modules/claude-collab/SKILL.md` |
| 要把项目迁到另一模型家族/换总窗（可中途单独装载） | `modules/migration/SKILL.md` |
| 重档正式项目 | `templates/` 四模板 → 项目 `docs/` |

模块的安装目标统一为 `./.agents/skills/<name>/SKILL.md`。复述清单、逐项给判定理由，并报告与现有文件的冲突；等待用户明确确认，确认前不写。

## 第 3 步：选择模式

- **临时**：只在当前会话读取并遵守选中文件，不写盘。
- **安装**：把薄核合并到项目根 `AGENTS.md`，把模块写入 `.agents/skills/`，重档模板写入 `docs/`。已有同名内容时展示差异并取得覆盖/合并授权。作用域仅限本项目目录，不影响其他项目与全局配置。

**写入方式**：薄核与模块一律用 shell 复制（`Copy-Item` / `cp`）源文件，随后逐字节比对；不用 `apply_patch` 写 `.agents/`——Codex 0.147 的 apply_patch 会以 "writing outside of the project" 拒绝该目录（本机实测 2026-08-18），这不是沙箱只读，换 shell 即可，不要向用户报"目录只读"。

交互会话可等待下一条确认。`codex exec` 第一次只提案并停止；确认后用 `codex exec --sandbox workspace-write resume <session-id> "已确认安装……"`（或新任务）继续。默认只读沙箱不能安装；托管策略仍可收窄权限。

## 第 4 步：执行并自述

执行后报告：

- 实际读取与写入的文件；
- 当前任务档位（轻/中/重）与理由；
- 当前协作档位（常规/额度紧张）；
- Claude 跨家族审查开关；
- 生效的局部覆盖、未解决冲突和已运行验证。

安装模式提示：以后重说激活句即可检查基线更新；更新前先显示 diff。

## 第 5 步：更新流

当用户要求“沉淀/更新基线”时，把通用经验改写为去项目化判据，列出受影响的两家族渲染与模块；经用户确认后再提交基线仓库（commit 信息说明来源与理由）。修改既有条款须保住效力等级，冲突不静默折中。
